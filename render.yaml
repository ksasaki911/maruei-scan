# Render.com Blueprint - マルエー商品スキャン
services:
  - type: web
    name: maruei-scan
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start_render.sh
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
